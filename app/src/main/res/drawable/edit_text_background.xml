<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/colorEditTextBackground"/>  <!-- Color de fondo -->
    <corners android:radius="8dp"/>  <!-- Bordes redondeados -->
    <stroke
        android:width="1dp"
        android:color="@color/colorBorder"/>  <!-- Color del borde -->
</shape>